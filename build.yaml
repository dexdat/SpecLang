pipeline:
  on_converge:
    - name: build
      run: npm run build
    - name: test
      run: npm test
      depends_on: [build]
  on_success: []
recovery:
  max_attempts: 3
  on_fail: []
convergence:
  quiet_period: 30
  max_iterations: 100
