# speclang-header lines:13
id: "@specs/assembler.spec.modified.spec"
version: 1.0.0
layer: 5
target: src/assembler.spec.modified.ts
project_level: Alpha
agent_support: agent_assonomous
tags: [modified, backup]
short: Parses spec headers/bodies, resolves @refs, extracts and writes assembled code
---

# assembler.spec.modified

Parses spec headers/bodies, resolves @refs, extracts and writes assembled code

### @block:assembler.spec.modified/code @kind:code
Parses spec headers/bodies, resolves @refs, extracts and writes assembled code
