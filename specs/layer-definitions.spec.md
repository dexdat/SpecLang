# speclang-header lines:10
id: "@speclang/layer-definitions"
version: 0.1.0
layer: 1
project_level: Alpha
agent_support: agent_autonomous
tags: [layer, definitions, autonomous, metadata]
children: ["@speclang/layer-definitions/abstraction", "@speclang/layer-definitions/examples"]
short: Concrete definitions for layer values 0-10
---
# Layer Definitions

Clear semantic mapping for the `layer` field (0-10 abstraction depth).

## Overview

This spec is split into two sub-specs:

- **Abstraction**: Layer table, usage guidelines, validation rules, and references
- **Examples**: Concrete definitions for each layer (0-10) with examples

See @ref:speclang/layer-definitions/abstraction for abstract definitions and @ref:speclang/layer-definitions/examples for concrete examples.